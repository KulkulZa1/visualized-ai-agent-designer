# AGENTS.md — Harness Studio — Active Project
_Generated by Harness Studio · 2026-05-16_

This file describes every agent in this harness, their roles, capabilities,
and how they communicate. Keep this file in sync with the workflow YAML.

---

## Topology overview

```
◆ Project Orchestrator → Task Router (task)
◇ Task Router          → Frontend Worker (ui?)  → Backend Worker (rust?)  → Test Worker (test?)  → Documentation Worker (doc?)
● Frontend Worker      → Test Gate  → Codebase Memory
● Backend Worker       → Test Gate  → Codebase Memory
● Test Worker          → Test Gate
◐ Visual Inspector     → Code Critic (visual-ok)  → Frontend Worker (visual-fix)
◐ Code Critic          → Documentation Worker (approved)  → Frontend Worker (code-fix)  → Backend Worker (rust-fix)
● Documentation Worker → Progress Log
▣ Codebase Memory      (terminal)
▣ Progress Log         (terminal)
✕ Test Gate            → Visual Inspector (gate-pass)
✕ Rate Limit Sentinel  → Project Orchestrator
```

## Agent definitions

### ◆ `Project Orchestrator`

| Field | Value |
|---|---|
| Role | Orchestrator |
| Model | `claude-opus-4.6` |
| Max tokens | 8,192 |
| Token budget | 64,000 |
| Max steps | 20 |
| Timeout | 300s |

**System prompt file:** ``

**Permitted tools:**
- `read_file`
- `list_files`
- `todo_write`
- `subagent_dispatch`

**Hooks:**
- Pre: `.harness/hooks/pre_run_consent.sh` (consent: true)

**Receives from:** Rate Limit Sentinel
**Sends to:** Task Router

**Reads memory:** `current-goal`, `test-results`, `review-verdict`, `visual-verdict`, `dev-history`
**Writes memory:** `task-plan`, `orchestrator-context`

---

### ◇ `Task Router`

| Field | Value |
|---|---|
| Role | Gateway |
| Model | `claude-haiku-4.5` |
| Max tokens | 1,024 |
| Token budget | 4,000 |
| Max steps | 5 |
| Timeout | 30s |
| Condition | `domain in {ui, rust, test, doc, mixed}` |

**System prompt:**

```
Classify the task into ui/rust/test/doc/mixed. Output JSON with domain and rationale.
```

**Permitted tools:**
- `classify`

**Receives from:** Project Orchestrator
**Sends to:** Frontend Worker, Backend Worker, Test Worker, Documentation Worker

**Reads memory:** `task-plan`
**Writes memory:** `routing-decision`

---

### ● `Frontend Worker`

| Field | Value |
|---|---|
| Role | Worker |
| Model | `claude-haiku-4.5` |
| Max tokens | 16,384 |
| Token budget | 80,000 |
| Max steps | 40 |
| Timeout | 600s |

**System prompt:**

```
Implement React/TypeScript changes following Atelier design system. CSS vars only, NodeIcon for icons, state in Zustand. Run tsc --noEmit after each file.
```

**Permitted tools:**
- `read_file`
- `fs.write`
- `fs.read`
- `fs.append`
- `list_files`
- `grep`
- `bash`

**Hooks:**
- Pre: `.harness/hooks/path_scope.py` (consent: false)

**Receives from:** Task Router, Visual Inspector, Code Critic
**Sends to:** Test Gate, Codebase Memory

**Reads memory:** `task-plan`, `routing-decision`, `review-verdict`
**Writes memory:** `frontend-changes`, `impl-status`

---

### ● `Backend Worker`

| Field | Value |
|---|---|
| Role | Worker |
| Model | `claude-haiku-4.5` |
| Max tokens | 12,288 |
| Token budget | 48,000 |
| Max steps | 30 |
| Timeout | 600s |

**System prompt:**

```
Implement Rust/Tauri changes. Always use resolve_safe_path(). Atomic writes only. Register commands in lib.rs. Run cargo build after each file.
```

**Permitted tools:**
- `read_file`
- `fs.write`
- `fs.read`
- `list_files`
- `grep`
- `bash`

**Hooks:**
- Pre: `.harness/hooks/destructive_guard.sh` (consent: false)

**Receives from:** Task Router, Code Critic
**Sends to:** Test Gate, Codebase Memory

**Reads memory:** `task-plan`, `routing-decision`, `review-verdict`
**Writes memory:** `backend-changes`, `impl-status`

---

### ● `Test Worker`

| Field | Value |
|---|---|
| Role | Worker |
| Model | `claude-haiku-4.5` |
| Max tokens | 8,192 |
| Token budget | 32,000 |
| Max steps | 20 |
| Timeout | 300s |

**System prompt:**

```
Write tests for changed code. Run npx vitest run and cargo test. Report N/M passing. Only mark done when all pass.
```

**Permitted tools:**
- `read_file`
- `fs.write`
- `fs.read`
- `list_files`
- `grep`
- `bash`
- `test`

**Receives from:** Task Router
**Sends to:** Test Gate

**Reads memory:** `frontend-changes`, `backend-changes`, `impl-status`
**Writes memory:** `test-results`

---

### ◐ `Visual Inspector`

| Field | Value |
|---|---|
| Role | Critic |
| Model | `claude-sonnet-4.6` |
| Max tokens | 8,192 |
| Token budget | 24,000 |
| Max steps | 10 |
| Timeout | 180s |

**System prompt:**

```
Review frontend changes for Atelier compliance: CSS vars, dark theme, spacing grid, node anatomy (header/prompt/footer/token bar). Return APPROVED or REVISE with specific fixes.
```

**Permitted tools:**
- `read_file`
- `fs.read`
- `grep`

**Receives from:** Test Gate
**Sends to:** Code Critic, Frontend Worker

**Reads memory:** `frontend-changes`, `test-results`
**Writes memory:** `visual-verdict`

---

### ◐ `Code Critic`

| Field | Value |
|---|---|
| Role | Critic |
| Model | `claude-opus-4.6` |
| Max tokens | 8,192 |
| Token budget | 32,000 |
| Max steps | 12 |
| Timeout | 240s |

**System prompt:**

```
Review for: architecture (Zustand state, typed IPC, file size ≤300 lines), security (no keys in code, path traversal, hook consent), code quality (no any, error handling, no TODOs). Return APPROVED / REVISE / ESCALATE.
```

**Permitted tools:**
- `read_file`
- `fs.read`
- `grep`
- `list_files`

**Receives from:** Visual Inspector
**Sends to:** Documentation Worker, Frontend Worker, Backend Worker

**Reads memory:** `frontend-changes`, `backend-changes`, `test-results`, `visual-verdict`
**Writes memory:** `review-verdict`

---

### ● `Documentation Worker`

| Field | Value |
|---|---|
| Role | Worker |
| Model | `claude-haiku-4.5` |
| Max tokens | 4,096 |
| Token budget | 16,000 |
| Max steps | 10 |
| Timeout | 120s |

**System prompt:**

```
Update docs after successful cycle. Follow existing format exactly. Update status, log entry, changelog.
```

**Permitted tools:**
- `read_file`
- `fs.write`
- `fs.read`

**Receives from:** Task Router, Code Critic
**Sends to:** Progress Log

**Reads memory:** `task-plan`, `frontend-changes`, `backend-changes`, `test-results`, `review-verdict`
**Writes memory:** `docs-updated`

---

### ▣ `Codebase Memory`

| Field | Value |
|---|---|
| Role | Memory |
| Model | `—` |
| Max tokens | 0 |
| Token budget | 32,000 |
| Max steps | 1 |
| Timeout | 10s |

**Permitted tools:**
- `fs.append`
- `fs.read`

**Receives from:** Frontend Worker, Backend Worker

**Writes memory:** `codebase-map`

---

### ▣ `Progress Log`

| Field | Value |
|---|---|
| Role | Memory |
| Model | `—` |
| Max tokens | 0 |
| Token budget | 16,000 |
| Max steps | 1 |
| Timeout | 10s |

**Permitted tools:**
- `fs.append`
- `fs.read`

**Receives from:** Documentation Worker

**Writes memory:** `dev-history`

---

### ✕ `Test Gate`

| Field | Value |
|---|---|
| Role | Hook |
| Model | `—` |
| Max tokens | 0 |
| Token budget | 0 |
| Max steps | 1 |
| Timeout | 120s |

**Permitted tools:**
- `bash`

**Hooks:**
- Pre: `.harness/hooks/test_gate.sh` (consent: false)

**Receives from:** Frontend Worker, Backend Worker, Test Worker
**Sends to:** Visual Inspector

**Reads memory:** `impl-status`
**Writes memory:** `gate-result`

---

### ✕ `Rate Limit Sentinel`

| Field | Value |
|---|---|
| Role | Hook |
| Model | `—` |
| Max tokens | 0 |
| Token budget | 0 |
| Max steps | 1 |
| Timeout | 10s |

**System prompt file:** ``

**Permitted tools:**
- `fs.read`
- `fs.write`

**Hooks:**
- Pre: `.harness/hooks/rate_limit_sentinel.py` (consent: false)

**Sends to:** Project Orchestrator

**Reads memory:** `rate-limit-status`
**Writes memory:** `rate-limit-status`

---

_Do not edit manually — regenerate via Harness Studio > Generate > AGENTS.md_