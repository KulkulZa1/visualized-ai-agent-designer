# Harness Studio — Active Project
_Generated by Harness Studio · 2026-05-16_
_Version: 1.0.0_

Production harness. Opus Orchestrator + Haiku Workers + Sonnet Visual Inspector + Opus Critic. GPT 5.5 fallbacks on rate limit.

---

## Project context

This project uses a multi-agent harness with **12** agents.
The workflow is defined in `.harness/harness-studio-—-active-project.harness.yaml`.
All config files live under `.harness/`. All edits should be reflected there.

## Agent roster

| Agent | Role | Model | Tools |
|---|---|---|---|
| ◆ **Project Orchestrator** | Orchestrator | `claude-opus-4.6` | `read_file, list_files, todo_write, subagent_dispatch` |
| ◇ **Task Router** | Gateway | `claude-haiku-4.5` | `classify` |
| ● **Frontend Worker** | Worker | `claude-haiku-4.5` | `read_file, fs.write, fs.read, fs.append, list_files +2` |
| ● **Backend Worker** | Worker | `claude-haiku-4.5` | `read_file, fs.write, fs.read, list_files, grep +1` |
| ● **Test Worker** | Worker | `claude-haiku-4.5` | `read_file, fs.write, fs.read, list_files, grep +2` |
| ◐ **Visual Inspector** | Critic | `claude-sonnet-4.6` | `read_file, fs.read, grep` |
| ◐ **Code Critic** | Critic | `claude-opus-4.6` | `read_file, fs.read, grep, list_files` |
| ● **Documentation Worker** | Worker | `claude-haiku-4.5` | `read_file, fs.write, fs.read` |
| ▣ **Codebase Memory** | Memory | `—` | `fs.append, fs.read` |
| ▣ **Progress Log** | Memory | `—` | `fs.append, fs.read` |
| ✕ **Test Gate** | Hook | `—` | `bash` |
| ✕ **Rate Limit Sentinel** | Hook | `—` | `fs.read, fs.write` |

## Agent responsibilities

### ◆ Project Orchestrator (Orchestrator)
> Reads PROJECT_STATUS.md + TODO.md, selects highest-priority task, routes to Task Router.

**System prompt:** see ``

**Allowed tools:** `read_file`, `list_files`, `todo_write`, `subagent_dispatch`
**Passes output to:** Task Router
**Hooks:** pre: `.harness/hooks/pre_run_consent.sh`
**Reads memory keys:** `current-goal`, `test-results`, `review-verdict`, `visual-verdict`, `dev-history`
**Writes memory keys:** `task-plan`, `orchestrator-context`

### ◇ Task Router (Gateway)
> Classifies task into domain (ui/rust/test/doc/mixed) and routes to appropriate workers.

**System prompt:**
```
Classify the task into ui/rust/test/doc/mixed. Output JSON with domain and rationale.
```

**Allowed tools:** `classify`
**Passes output to:** Frontend Worker, Backend Worker, Test Worker, Documentation Worker
**Reads memory keys:** `task-plan`
**Writes memory keys:** `routing-decision`

### ● Frontend Worker (Worker)
> React/TypeScript changes. Atelier design tokens. No hex colors. IPC through tauriCommands.ts.

**System prompt:**
```
Implement React/TypeScript changes following Atelier design system. CSS vars only, NodeIcon for icons, state in Zustand. Run tsc --noEmit after each file.
```

**Allowed tools:** `read_file`, `fs.write`, `fs.read`, `fs.append`, `list_files`, `grep`, `bash`
**Passes output to:** Test Gate, Codebase Memory
**Hooks:** pre: `.harness/hooks/path_scope.py`
**Reads memory keys:** `task-plan`, `routing-decision`, `review-verdict`
**Writes memory keys:** `frontend-changes`, `impl-status`

### ● Backend Worker (Worker)
> Rust/Tauri backend. resolve_safe_path(), atomic writes, AppError, cargo build after changes.

**System prompt:**
```
Implement Rust/Tauri changes. Always use resolve_safe_path(). Atomic writes only. Register commands in lib.rs. Run cargo build after each file.
```

**Allowed tools:** `read_file`, `fs.write`, `fs.read`, `list_files`, `grep`, `bash`
**Passes output to:** Test Gate, Codebase Memory
**Hooks:** pre: `.harness/hooks/destructive_guard.sh`
**Reads memory keys:** `task-plan`, `routing-decision`, `review-verdict`
**Writes memory keys:** `backend-changes`, `impl-status`

### ● Test Worker (Worker)
> Writes Vitest + Rust tests for changed code. Runs full suite. Reports pass/fail counts.

**System prompt:**
```
Write tests for changed code. Run npx vitest run and cargo test. Report N/M passing. Only mark done when all pass.
```

**Allowed tools:** `read_file`, `fs.write`, `fs.read`, `list_files`, `grep`, `bash`, `test`
**Passes output to:** Test Gate
**Reads memory keys:** `frontend-changes`, `backend-changes`, `impl-status`
**Writes memory keys:** `test-results`

### ◐ Visual Inspector (Critic)
> Reviews frontend changes against Atelier design system. Checks CSS tokens, spacing, dark theme, node anatomy.

**System prompt:**
```
Review frontend changes for Atelier compliance: CSS vars, dark theme, spacing grid, node anatomy (header/prompt/footer/token bar). Return APPROVED or REVISE with specific fixes.
```

**Allowed tools:** `read_file`, `fs.read`, `grep`
**Passes output to:** Code Critic, Frontend Worker
**Reads memory keys:** `frontend-changes`, `test-results`
**Writes memory keys:** `visual-verdict`

### ◐ Code Critic (Critic)
> Architecture + security + quality gate. Returns APPROVED, REVISE, or ESCALATE.

**System prompt:**
```
Review for: architecture (Zustand state, typed IPC, file size ≤300 lines), security (no keys in code, path traversal, hook consent), code quality (no any, error handling, no TODOs). Return APPROVED / REVISE / ESCALATE.
```

**Allowed tools:** `read_file`, `fs.read`, `grep`, `list_files`
**Passes output to:** Documentation Worker, Frontend Worker, Backend Worker
**Reads memory keys:** `frontend-changes`, `backend-changes`, `test-results`, `visual-verdict`
**Writes memory keys:** `review-verdict`

### ● Documentation Worker (Worker)
> Updates PROJECT_STATUS.md, DEVELOPMENT_LOG.md, CHANGELOG.md after each approved cycle.

**System prompt:**
```
Update docs after successful cycle. Follow existing format exactly. Update status, log entry, changelog.
```

**Allowed tools:** `read_file`, `fs.write`, `fs.read`
**Passes output to:** Progress Log
**Reads memory keys:** `task-plan`, `frontend-changes`, `backend-changes`, `test-results`, `review-verdict`
**Writes memory keys:** `docs-updated`

### ▣ Codebase Memory (Memory)
> Persists codebase map (key files, types, patterns) across sessions. Path .harness/memory/codebase.jsonl.


**Allowed tools:** `fs.append`, `fs.read`
**Writes memory keys:** `codebase-map`

### ▣ Progress Log (Memory)
> Append-only journal of every dev cycle. Enables multi-session continuity. Path .harness/dev-progress.jsonl.


**Allowed tools:** `fs.append`, `fs.read`
**Writes memory keys:** `dev-history`

### ✕ Test Gate (Hook)
> Runs tsc + vitest + cargo test before inspectors. Blocks on any failure.


**Allowed tools:** `bash`
**Passes output to:** Visual Inspector
**Hooks:** pre: `.harness/hooks/test_gate.sh`
**Reads memory keys:** `impl-status`
**Writes memory keys:** `gate-result`

### ✕ Rate Limit Sentinel (Hook)
> Checks .harness/state/rate_limit.json. Sets CLAUDE_RATE_LIMITED=1 if Anthropic is limited so workers use fallback models.

**System prompt:** see ``

**Allowed tools:** `fs.read`, `fs.write`
**Passes output to:** Project Orchestrator
**Hooks:** pre: `.harness/hooks/rate_limit_sentinel.py`
**Reads memory keys:** `rate-limit-status`
**Writes memory keys:** `rate-limit-status`

## Security notes

The following agents have elevated permissions. Exercise caution:
- **Project Orchestrator**: `subagent_dispatch`
- **Frontend Worker**: `fs.write, bash`
- **Backend Worker**: `fs.write, bash`
- **Test Worker**: `fs.write, bash`
- **Documentation Worker**: `fs.write`
- **Test Gate**: `bash`
- **Rate Limit Sentinel**: `fs.write`

All hook executions require user consent and are logged to `.harness/audit.log.jsonl`.

## Execution settings

- Max parallel agents: **3**
- Workflow timeout: **3600s**
- Retry on failure: **true**

---
_Do not edit this file manually — regenerate via Harness Studio > Generate > CLAUDE.md_