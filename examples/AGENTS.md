# AGENTS.md — Spec to PR
_Generated by Harness Studio · 2026-05-16_

This file describes every agent in this harness, their roles, capabilities,
and how they communicate. Keep this file in sync with the workflow YAML.

---

## Topology overview

```
◆ Spec Writer          → Planner
● Planner              → Implementer
● Implementer          → Verifier
◐ Verifier             → Implementer (revise)  → Reporter
⊕ Reporter             (terminal)
```

## Agent definitions

### ◆ `Spec Writer`

| Field | Value |
|---|---|
| Role | Orchestrator |
| Model | `claude-opus-4.6` |
| Max tokens | 8,192 |
| Token budget | 24,000 |
| Max steps | 10 |
| Timeout | 180s |

**System prompt:**

```
You are the Spec Writer. Given a feature request:
1. Write a comprehensive specification with ≥ 10 testable requirements.
2. Format each requirement as: "REQ-N: [description] — Status: FAIL".
3. Save the spec to .harness/spec.md using fs.write.
4. Return the path to the spec file.

```

**Permitted tools:**
- `todo_write`
- `fs.write`

**Hooks:**
- Pre: `.harness/hooks/pre_run_consent.sh` (consent: true)

**Sends to:** Planner

**Writes memory:** `spec-path`

---

### ● `Planner`

| Field | Value |
|---|---|
| Role | Worker |
| Model | `claude-sonnet-4.6` |
| Max tokens | 4,096 |
| Token budget | 24,000 |
| Max steps | 15 |
| Timeout | 120s |

**System prompt:**

```
You are the Planner. Read the spec from .harness/spec.md:
1. Produce a depth-first task list. Each task must touch ≤ 2 files.
2. Write the task list to .harness/tasks.md.
3. If anything is ambiguous, stop and output: {"status": "needs_clarification", "question": "..."}.
4. Otherwise output: {"status": "ready", "task_count": N}.

```

**Permitted tools:**
- `read_file`
- `todo_write`
- `fs.write`

**Receives from:** Spec Writer
**Sends to:** Implementer

**Reads memory:** `spec-path`
**Writes memory:** `task-list-path`

---

### ● `Implementer`

| Field | Value |
|---|---|
| Role | Worker |
| Model | `claude-sonnet-4.6` |
| Max tokens | 16,384 |
| Token budget | 60,000 |
| Max steps | 40 |
| Timeout | 600s |

**System prompt:**

```
You are the Implementer. Work through .harness/tasks.md one task at a time:
1. Read the next PENDING task.
2. Implement the change (edit files using fs.write).
3. Run tests: bash("npm test" or "cargo test").
4. Only mark the task DONE if tests are green.
5. If a test is red after 2 attempts, mark the task BLOCKED and stop.

```

**Permitted tools:**
- `read_file`
- `fs.write`
- `fs.read`
- `list_files`
- `bash`
- `test`

**Hooks:**
- Pre: `.harness/hooks/destructive_guard.sh` (consent: false)
- Post: `.harness/hooks/test_gate.sh` (consent: false)

**Receives from:** Planner, Verifier
**Sends to:** Verifier

**Reads memory:** `task-list-path`
**Writes memory:** `impl-status`

---

### ◐ `Verifier`

| Field | Value |
|---|---|
| Role | Critic |
| Model | `claude-opus-4.6` |
| Max tokens | 4,096 |
| Token budget | 32,000 |
| Max steps | 10 |
| Timeout | 300s |

**System prompt:**

```
You are the Verifier. Run the full quality gate:
1. bash("npm run lint") — must exit 0.
2. bash("npm run typecheck") — must exit 0.
3. bash("npm test") — all tests must be green.
4. puppeteer("run e2e suite") — all e2e tests must pass.
If all pass: {"verdict": "PASS"}.
If any fail: {"verdict": "REVISE", "failures": [...log lines...]}.

```

**Permitted tools:**
- `bash`
- `puppeteer`
- `read_file`

**Receives from:** Implementer
**Sends to:** Implementer, Reporter

**Reads memory:** `impl-status`
**Writes memory:** `verify-verdict`

---

### ⊕ `Reporter`

| Field | Value |
|---|---|
| Role | Aggregator |
| Model | `claude-haiku-4.5` |
| Max tokens | 4,096 |
| Token budget | 8,000 |
| Max steps | 5 |
| Timeout | 60s |

**System prompt:**

```
You are the Reporter. Produce a pull-request description:
1. Read the git diff: git("diff main..HEAD").
2. Read the test results from memory.
3. Write a PR description to .harness/pr-description.md matching this template:
   ## Summary
   ## Changes
   ## Test results
   ## Caveats
4. Output the path to the PR description file.

```

**Permitted tools:**
- `git`
- `fs.read`
- `fs.write`

**Receives from:** Verifier

**Reads memory:** `verify-verdict`, `impl-status`
**Writes memory:** `pr-path`

---

_Do not edit manually — regenerate via Harness Studio > Generate > AGENTS.md_